# Blackwell 六路 BJT 只读实验结果

## 结论

2026-07-19 已在本机 RTX 5090 上完成端到端验证：自建 WDM 驱动经本机测试证书签名后，被 Windows TESTSIGNING 模式成功加载；Python 可连续轮询六个 BJT 寄存器，并按六路有效温度的最大值计算 Hot Spot。

## 实测结果

- GPU BAR0：`0x00000000D8000000`，与加载前 PnP 资源核对一致。
- 六个寄存器：`BAR0 + 0xAD0A90`、`0xAD0A94`、`0xAD0A98`、`0xAD0A9C`、`0xAD0AA0`、`0xAD0AA4`。
- 有效掩码：每次均为 `0x3F`，六路全部有效。
- 解码：bit 30 表示有效；温度为 `(raw & 0xFFFF) / 256` °C。
- 首轮十秒采样：六路约 `46.38–51.09°C`，Hot Spot 为 `50.69–51.09°C`。
- UTF-8 输出复验：四次样本全部有效，Hot Spot 为 `48.88–49.38°C`。
- 非提升 Python 进程打开设备时返回 `WinError 5`，管理员只读 ACL 生效。

## 驱动状态与边界

- 服务名：`CatBjtReadOnly`。
- 启动类型：`DEMAND_START`，不会随系统自动启动。
- 驱动没有定时器或后台轮询；仅在 Python 发出 IOCTL 时映射一页，读取完成后立即解除映射。
- 没有任意地址、任意偏移、任意长度或写入入口。
- 本实现只适用于当前机器和当前 BAR0 分配，没有合并到 CAT。

复现和卸载方法统一见 `README.md`。
